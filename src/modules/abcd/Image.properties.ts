import type { HTMLAttributesImage } from '@framework'
import type Properties from '@modules/ABCD.properties'

/**
 * TODO in the next project, loading=lazy|intersection|background
 */
export default interface Image extends Properties, HTMLAttributesImage { }
